
# pragma once

#include <cstring>
#include <iostream>
#include <sstream>
#include <string>

class User
{
protected:
	const std::string _nickname; // values are change-able so not const take a look what can be changed
	const std::string _username;
	const std::string _hostname;
	const std::string _realname;

public:


};